const path = require('path')
const { StatusCodes } = require('http-status-codes')

const uploadProductImage = async (req, res) => {


    //     const localPath = `/uploads/${files.name}`
    //     return localPath
}

module.exports = uploadProductImage